1) what is inheritence ?
=> inheritence is a objective

there are three types of inheritense
=> hirachical inheritence
=> hybrid inheritence
=> multilevel inheitence
= singlelevel inheritence.

2) which inheritence is not supported bt dart ?
=> dart is not support multilevel inheritence.
=>  multiple inheritence :

    => the multiple inheritence :
    => the multiple inheritence is not support by dart wht
    => multiple inheritence is a a class inherit another child class.
    => is the not support by dart.

3)  what is different between inheritenceand encapsulation ?
=> inheitence's code reuse and support hirachical relationship between class.
=> encapsulation provide controlled acces to data and behavior on project.

4) what is different between inheitence and polymorphism ?

=> inheitence's code reuse and support hirachical relationship between class.
=> polymorphism one name many form.

5) can we override static method in dart ?
=> not available override static method in dart .


6) can we overoad static mehod in dart ?

=> not available overload method in dart.


7) can a class implement more then one interface ?

=> yes a class implement more then in daart.

8) can an interface extend more then one interface in dart ?
not available.

9) What will happen if a class implements two interfaces and they both have a method with same name and signature? 

=> a class implement two interface and they both have a method with same name and signature .


10) Can we pass an object of a subclass to a method expecting an object of the super class?
=> yes we cam pass object of a subclass to a method expe and object of the super class.

11) What happens if the parent and the child class have a field with same identifier?

=> the reference hold the child class object reference will not  be able acces to the member of child class.

12) How do you restrict a member of a class from inheriting by its sub classes?

=> restrict a member of a class from inheriting bt its sub classes .
   => use the keyword in class.

13) How do you implement multiple inheritance in Dart ?

=> dart has not support by multiple inheritence .

14) Can a class extend by itself in Dart  ?

=> not extend by itself in dart 

15) How do you override a private method in Dart  ?

=> override a private method in dart .
  => create a private method in class.

  16) When to overload a method in Dart and when to override it ?

  => overload a method in dart and to override .
      => a method in class same name and same parametre.

17) What the order is of extends and implements keyword on Dart class declaration  ?

=> the order is of ectend and implement keyword on dart class .
   => class name extend second class name {
    @override
   }

18) How do you prevent overriding a Dart method without using the final modifier  ?

=> You can also use static and private modifier to prevent overriding a dart method without using the final modifier

19_) What are the rules of method overriding in Dart ?

=> the class method of overriding in dart 
  => The return type, list of arguments and its sequence must be the same as the parent class method.


20) Difference between method overriding and overloading in Dart ?

overloading has two or more method same name and same paramtre has same it is the overriding.
=> overriding is two or more method same namr , parametre has not same it is the overloading.

21) What happens when a class implements two interfaces and both declare field (variable) with same name ?

=> a class implements two interface and both declarw field variab;le with same name.

22) Can a subclass instance method override a superclass static method ?

=> not static method override a superclass  static method.

23) Can a superclass access subclass member?
=>   Yes, a superclass can access subclass member.

